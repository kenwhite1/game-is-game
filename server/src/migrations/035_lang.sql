-- Язык интерфейса и уведомлений бота, выбранный игроком (§i18n).
-- 'ru' по умолчанию; клиент присылает выбор в POST /profile/lang, а бот берёт
-- отсюда язык для пушей (последний шанс, оплаты, ответы на команды).
ALTER TABLE users ADD COLUMN lang TEXT NOT NULL DEFAULT 'ru';
